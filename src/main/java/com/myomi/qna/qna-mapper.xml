<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="QnaMapper">
	<select id="getQnaList" resultType="qna">
		SELECT
 		num AS num, 
 		user_id AS userId, 
 		prod_num AS prodNum, 
 		que_title AS queTitle, 
 		que_content AS queContent, 
 		que_created_date AS queCreatedDate,
 		ans_content AS ansContent,
 		ans_created_date AS ansCreatedDate
 		FROM qna
 		<!-- ORDER BY num; -->
	</select>
	

</mapper>