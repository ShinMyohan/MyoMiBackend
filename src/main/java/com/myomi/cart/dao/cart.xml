<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="CartMapper">
	<!-- 장바구니에 상품 추가 -->
	<insert id="insertCart">
		INSERT INTO cart (num, user_id, prod_num, prod_cnt)
		VALUES(seq_cart.nextval, #{user.id}, #{product.num}, #{prodCnt})
	</insert>
	
	<!-- 장바구니에 담긴 상품 목록 조회 -->
	<select id="selectAllCart" resultType="java.util.LinkedHashMap">
		<!-- SELECT * 
		FROM cart 
		WHERE user_id = #{user.id}
		ORDER BY NUM -->
		SELECT c.*, p.name, p.num prodNum, p.percentage, p.origin_price
		FROM cart c
		LEFT JOIN product p ON c.prod_num = p.num
		WHERE c.user_id = #{user.id}
	</select>
	
	<!-- 장바구니에 담긴 상품 수량 수정 -->
	<update id="updateCart">
		UPDATE cart
		SET prod_cnt = #{prodCnt}
		WHERE num = #{num}
	</update>
	
	<!-- 장바구니에서 상품 삭제 -->
	<delete id="deleteCart">
		DELETE FROM cart
		WHERE num IN (
		<foreach collection="list" item="num" separator=",">
		#{num}
		</foreach>
		)
	</delete>
	<!-- <delete id="deleteCart" parameterType="string">
		DELETE FROM cart
		<where>
		FIND_INT_SET(num,#{num})
		</where>
	</delete> -->
	
</mapper>
