<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.myomi.product.dao.ProductDAO">
	<resultMap id="Product" type="Product" autoMapping="true">
			<id property="num" column="num" javaType="int"/>
			<result property="sellerId" column="seller_id"/>
			<result property="category" column="category"/>
			<result property="name" column="name"/>
			<result property="originPrice" column="origin_price"/>
			<result property="percentage" column="percentage"/>
			<result property="week" column="week"/>
			<result property="status" column="status"/>
			<result property="detail" column="detail"/>
			<result property="reviewCnt" column="review_cnt"/>
			<result property="stars" column="stars"/>
			<result property="fee" column="fee"/>
			<!-- 1:1 관계에서는 assocication -->
			<!-- <collection property="qna" column="num" javaType="java.util.ArrayList" 
			ofType="com.myomi.qna.QnaVo" select="ProdMapper.getQnaList"/> -->
	</resultMap>
	
	<!-- <resultMap type="Qna" id="Qna">
		<id property="num" column="num" javaType="int"/>
		   <result property="userId" column="user_id" />
		   <result property="prodNum" column="prod_num" />
		   <result property="queTitle" column="que_title" />
		   <result property="queContent" column="que_content" />
		   <result property="queCreatedDate" column="que_created_date" />
		   <result property="ansContent" column="ans_content" />
		   <result property="ansCreatedDate" column="ans_created_date" /> 
	</resultMap> -->
	
	<!-- <select id="selectByProdQna" resultMap="Product">
		 SELECT p.num, p.name, q.num, q.user_id, q.prod_num, q.que_title, que_content
		 FROM product p, qna q 
		 WHERE p.num = 2
	</select> -->
	
	<!-- <select id="getQnaList" resultType="qna">
		SELECT
 		num, 
 		user_id, 
 		prod_num, 
 		que_title, 
 		que_content, 
 		que_created_date,
 		ans_content,
 		ans_created_date
 		FROM qna
 		ORDER BY num;
	</select> -->
	<select id="selectAllProds" resultType="Product">
		SELECT
 		num, 
 		seller_id, 
 		category, 
 		name, 
 		origin_price, 
 		percentage,
 		week,
 		status,
 		detail,
 		review_cnt,
 		stars,
 		fee
 		FROM product
 		<!-- ORDER BY num; -->
	</select>
</mapper>